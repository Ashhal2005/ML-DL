What i learned from this
1. Highly Correlated features reduce the performance of KNN.
   it is better to drop one of highly related features.
2. KNN is a lazy learner. For huge dataset it has to calculate distance from each point.
3. KNN is the Algorithm that has no training. it calculates distance in prediction 
   and classify the input to the group with majority in closest K data point.
4. Value to k should be ODD. 